// If a match is detected, then, redirect to external website...

document.body.innerHTML = "<h1>Redircting to sandbox</h1>" + window.top.location.href;

// 
if(navigator.platform == "MacIntel") {
	//alert(file);
}